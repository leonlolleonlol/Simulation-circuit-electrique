<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            background-color: <%=user.color%>;
        }
      </style>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg" href="/images/icons/cpu.svg">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"/>
    <title>Dashboard</title>
</head>
<body>
    
    <div id="user-info">
        <a href="/users/logout" class="w3-bar-item w3-button">
            <img src="../images/icons/log-out.svg" alt="Déconnexion"/>
        </a>
    </div>
        <br></br>
    <a href="/editeur">
        <button class="btn" style="font-size: 28px;">Ouvrir l'éditeur
        </button>
    </a>
        <br></br>
    <div id="user-info">
        <h1> Bonjour <%= user.prenom.charAt(0).toUpperCase() + user.prenom.slice(1) %> !</h1>
    </div>
    <br>
    <h2>Projets</h2>
    <% if (user.details>0) { %>
        <h3>Vous n'avez jamais sauvegardé des circuits.</h3>
      <% } else { %>
        <h3>Votre dernier circuit est sauvegardé!</h3>
      <% } %>
    <script>
        function sendSaveCircuit(projet, user){
            fetch('/editeur', {
                method: 'POST',
                body: user,
                headers: {
                    "Content-Type": "application/json",
                },
            });
        }
    </script>
    
</body>
</html>